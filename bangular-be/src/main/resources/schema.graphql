schema {
    query: Query
}

type Query {
    getUsers: [User!]!
    getUserByUsername(username: String!): User
}

type User {
    id: ID!
    name: String!
    username: String!
    email: String!
    roles: [Role!]
}

type Role {
    id: ID!
    name: String!
}
